events {}

http {
  server {
    listen 80;
    location / {
      root /usr/share/nginx/html;
      index index.html;
    }
    # Всі запити, що починаються з /api/
    location /api/ {
      proxy_pass http://server:3020;
    }
  }
}